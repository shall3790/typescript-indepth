export class ArrayExample {
    // using string data type
    strArray1: string[] = ['here', 'are', 'strings'];

    // using generics syntax
    strArray2: Array<string> = ['more', 'strings', 'here'];

    // use any keyword ;-)
    anyArray: any[] = [42, true, 'banana'];

    constructor() {}
}
